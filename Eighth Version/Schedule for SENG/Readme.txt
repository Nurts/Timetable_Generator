Course Faculty.txt
	CourseName(space)Number of hours(space)Profs teching this course(separate with spaces) (space);
	SecondCoursename....

Room Capacity.txt
	RoomName(Space)Capacity
	RoomName2....

Year Major Course.txt
	YearNumber(space)MajorName(space)NumberofGroupsInThisMajor(space)Number of students in each group(space)Courses which are taken by this Major(with spaces) ;

All Names Should be without spaces use '_'
You can change .txt files